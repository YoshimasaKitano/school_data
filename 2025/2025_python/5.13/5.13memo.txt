Python
インタプリタ型言語
書いたソースコードをそのまま実行していく
対話型、ソースファイルを作っていく型
例：PHP

コンパイラ型言語
例：C言語、C#、Java

Pythonの特徴
Pythonは一番勉強しやすい言語
プログラミングをはじめたての人には一番優しい言語
汎用性が高い
ウェブアプリケーションの作成、AI、機械学習、業務の自動化に作成
コードを書いてすぐに実行できる
ライブラリがたくさんある→細かいプログラムを作らなくてもライブラリからコマンドを読み込んでプログラムを書くことができる
オープンソース→誰でも自由にインストールできる

IDLE→ただでPythonがかけるツール

>>>→対話型のプログラム

計算→足し算、引き算よりも、掛け算、割り算が先に計算される→演算子の優先順位
→前から順番に実行されていくが、四則演算のルールは守ってくれる

変数に値をすぐ代入できる→変数宣言もなく、型指定もない

kosu=12*5
kosu→変数
=→代入するという意味(この場合この計算結果をkosuに代入するという意味になる)

代入された変数を打つと代入した値が出てくる

# →赤文字になったところがコメント(メモ)になる
※空白忘れないように

変数同士の計算
それぞれの変数に値を代入し変数同士計算する

変数の上書き
新しく変数に値を代入する
※変数同士の計算を変数を上書きしてからするとき、求める式をもう一度実行する必要がある
→
red_ball=5
white_ball=7
all = red_ball + white_ball
all
12
red_ball = 3
〇
all
12
になってしまうので〇のところで必ずもう一度all = red_ball + white_ballを定義する必要がある

Ctrl+N→新しいファイルの作成

.py→Pythonの拡張子

ファイルの左上に*がついているときは保存されていないとき
Ctrl+S→保存

F5→Runモジュール

print(〇〇)→〇〇を出力させるコマンド
→ファイルから実行する場合はこのコマンドがないと表示されない

1行のことをステートメント
→PHPのように文末に;が入らない
複数の文章を一つのステートメントに書く際には;で区切って書く
→見にくいのであまりこの方法では書かない(各会社のコーディングルールに従う)

ソースコードは自分のために書くものではない
→クライアント、チームのために書く
→自分しかわからないコードではなく誰にでも見やすいコードを書く

例;### 〇〇(実行結果)
→わかりやすく実行結果をコメントでまとめておく

Windowsではバックスラッシュが¥になる
\でステートメントを改行する
改行したら>>>が...になっている

len→オブジェクトの長さを表す関数

"""
複数行のコメント(メモ)→説明書きなどに使う

"""
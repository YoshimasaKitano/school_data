/* 20250819 */
order by →並び変える

desc→降順に並び替える

asc→昇順に並び替える

ascは省略しても昇順に並び変わる→デフォルトで昇順に並び替えるため

アルファベットや平仮名は比較的並び替えてくれる→漢字は無理

名前とかよりもコードとかアルファベットだけのものを並び替えたりするほうが使われる

as→名前をつけられる

1行のデータと複数のデータをせっで出力することができない

count()→行数

sum()→合計

avg()→平均

max()→最大値

min()→最小値

round()→まるめ処理

truncate(), 〇→〇で切り捨て

abs()→絶対値

sort()→平方根

lower()→文字列を小文字に変換

upper()→文字列を大文字に変換

length()→文字列の長さをバイト数で取得

char_length()→文字列の長さを文字数で取得

group by→グループ化する
→かぶっているものなどを全部集約してくれる

join→テーブルをくっつける

on→合わす基準となるところを示す

limit 〇→上位〇だけ取得する

テーブルの同じ列をselectでとってくるとき、左のテーブルのほうからとってきたほうが形としてきれい
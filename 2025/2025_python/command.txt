kosu=12*5
kosu→変数
=→代入するという意味(この場合この計算結果をkosuに代入するという意味になる)

# →赤文字になったところがコメント(メモ)になる
※空白忘れないように

Ctrl+N→新しいファイルの作成

Ctrl+S→保存

F5→Runモジュール

print(〇〇)→〇〇を出力させるコマンド
→ファイルから実行する場合はこのコマンドがないと表示されない

例;### 〇〇(実行結果)
→わかりやすく実行結果をコメントでまとめておく

len→オブジェクトの長さを表す関数

"""
複数行のコメント(メモ)→説明書きなどに使う

"""
print(〇,〇,〇)→〇 〇 〇と表示される

sep = "〇"→〇の方法で区切る

end =""→endがついている一つ下の部分だけ改行されずに入力される

print()→これだけで打つと開業の役割を担う

round(〇〇)→四捨五入みたいなやつ。小数点以下の値をまとめる。頭が偶数の場合はその値のほうに引っ張られる。
※Excelのroundとは違う

2進数では0bをつける

8進数では0oをつける

16進数では0xをつける

//→小数点を切り捨てる

%→あまり

**→累乗

"〇〇\"〇〇\"〇〇"→""の中に""を入れるとき"の前に\を入れる

\n→改行

print("\\")→\が表示される

text"""
"""→複数行の文字列をかける

str(〇)→〇という数値を文字列に変換する
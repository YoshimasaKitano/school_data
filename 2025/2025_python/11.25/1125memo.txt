Flask→Pythonで簡単にWebアプリケーションを作れる
→セッションを作らなければならなかったりするので、簡単に作れるが手間も多い

MVC→モデル・ビュー・コントローラー
→最終的にHTMLに渡して表示する

本来であればapp.pyとルーティング用のファイルは分けたほうがよい

render_template()を使うときは、templatesのようにディレクトリは複数形にしておかないと動かない

同じテンプレートフォルダの中の別のhtmlファイルには直接リンクでアクセスできないようになっている
20250425

ip addr show→ip a
IPアドレスの取得ができる

teraterm起動後、すぐにファイルからログでログ保存を行う
datapabのlogsに保存

cmd→コマンドプロンプト

↑一つ前のコマンドをそのままコピペできる

Page8 仮想マシンを作成やってもいい

ディレクトリ

シャットダウンの仕方Page18


シェル→キーボード(キーボード入力→コマンド)やマウスなどの入出力を担う(インターフェイス(窓口))
↓
OSカーネルにシェルが入出力をする

echo→これの後ろに文字をうつと表示させられる

存在しないコマンドを打つと command not found

bash→シェルの種類(名前)

hostname→コンピュータの名前を確認するコマンド

管理者状態からexit→一般ユーザーに降格

管理者状態でファイルを作るとユーザーになったときにそのファイルを開けなくなる可能性があるので注意

clear→きれいにする

echo $SHELL→ログインシェルの確認コマンド

新しいシェルを開く→Windowsでもう一つWindowsを開いたイメージ

exitでシェルから抜けれる
ログインシェルからexitするとlogoutされる

dnf install 〇〇(とってきたいコマンド) -y→コマンドをとってくるコマンド
※管理者権限必須
installのところをremoveにすると逆に消してくれる

-y→途中で出てくるyes,noをすべてyesで通してくれるようにする

ping 8.8.8.8→ネットに接続できているかわかるコマンド(googleのdnsサーバーのアドレス)

ctrl+c,ctrl+q→すべてをとめるコマンド

pwd→自分の位置を確認できるコマンド
　　 現在の作業ディレクトリを絶対パスで表示する。

ls→ファイルの中身やディレクトリの中身を確認するコマンド
これに -aをつけると隠しファイルも見える

..→一つ上のディレクトリを表す

ls -l→詳細情報がでてくる

cd→チェンジディレクトリ

相対パス指定…今作業している位置から見た時のファイルの場所のこと
　　　　　　　今現在のディレクトリを基準としたファイルやディレクトリの位置情報
(例)cd ../../etc

絶対パス指定…/ ルートを基準としたファイルやディレクトリの位置情報
(例)cd /etc

相対パスと絶対パスの違い
自分の今の位置から見るか、ルートから見るかの違い


lsの後ろに引数をかくとその引数の中身を確認することができる

~…チルダ→userhomeにいるということ

Tabキーで頭文字から予測変換で勝手に打ってくれる

history→これまでに打ったコマンドを表示してくれるコマンド

 !〇(hisoryの数字)→そのコマンドの実行

ctrl+u→打った文字を消す

ctrl+a→行頭に移動する

ctrl+k→カーソルの右側を消す

ctrl+y→消した文字を貼り付け

ctrl+e→行末に移動する

ctrl+h→一文字消す

ctrl+w→スペースごとに消す

ctrl+l→clear

ctrl+s→画面ロック

〇TabTab→〇から始まるコマンド単語を表示してくれるコマンド

bin→実行形式のファイル

 | grep 〇→絞り込み

touch→空のファイルの作成とタイムスタンプの更新

rm→ファイルの削除

rm -i→ファイルの削除をすることに許可を求めてくる
聞かれた後にyなど入力すると消してくれる
nでキャンセル

ワイルドカード
*:0文字以上の任意の文字列
?:1文字の任意の文字列

newfile??にすると後ろに二桁の文字がついているファイルを消してくれる
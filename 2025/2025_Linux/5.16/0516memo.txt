カレントディレクトリ→現在作業しているディレクトリのこと

TeraTermはターミナルソフト→コンピュータに対して遠隔操作するためのソフトウェア

Linixの特徴
オープンソースである
有償ライセンスが必要な商用ディストリビューションも存在する
信頼性が高い
容量は小さい

メインフレーム→主に大企業や官公庁などの基幹業務システムに用いられる大型コンピュータのこと

mkdirコマンドで間のディレクトリがなくてもファイルを作成するには -pをつける

cat 〇〇→〇〇というファイルの中身を表示

cat 〇〇 -n→ファイルの中身を行番号を付けて表示してくれる

catコマンドでは、複数のファイルを連結して表示させることができる

catだけで打つと返答botになり終了するにはCtrl+DまたはCtrl+C

tac 〇〇→〇〇の中身をcatとは逆の順番で表示してくれる(ネタ枠)

シェルスクリプト→Linuxのプログラムが作れる

less 〇〇→〇〇ファイルをページ単位でスクロールしてくれる
→終わるときはCtlr+CまたはQ

less -N 〇〇→行番号もつけられる

ls -F→ディレクトリに/をつけられる

cp -i→上書き確認

dnf list installed→インストールされているものを確認できる

| grep 〇〇→〇〇が入っている単語でしぼれる

ExcelでAlt+Enterでセルの中で改行できる

httpのポート番号は80
httpsのポート番号は443

firewall-cmd --add-port=80/tcp --zone=public --permanet

ドキュメントルート→Webサーバ(HTTPサーバ)やFTPサーバが外部に公開するファイルなどが置かれたディレクトリ(フォルダ)